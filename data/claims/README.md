# data/claims

Structured claim records live here as they are promoted out of docs/TESTABLE_CLAIMS.md.

First-pass register remains the markdown table. JSON/CSV rows may be added later without changing claim IDs.
